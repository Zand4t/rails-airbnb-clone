<h1>Streams Index</h1>
<p>This should list all the streams in the db</p>
<% @streams.each do |stream| %>

  <h3><%= stream.name%></h3>
  <%= stream.link%>
  <p>
  <%= link_to 'Show', stream %>
  </p>
<% end %>
<%= link_to 'Create a new Stream', new_stream_path %>
