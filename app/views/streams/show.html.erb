<% @streams = Stream.find(params[:id])%>

<h1><%= @streams.name %></h1>
<p><%= @streams.link %></p>
<p><%= @streams.description %></p>
<p>
     <%= link_to 'Edit Stream', edit_stream_url %>
     <br>
      <%= link_to 'Destroy Stream', @stream , method: :delete, data: { confirm: 'Delete Stream?'} %>
     <br>
     <%= link_to 'Go Back to Index', streams_path %>
     </p>

